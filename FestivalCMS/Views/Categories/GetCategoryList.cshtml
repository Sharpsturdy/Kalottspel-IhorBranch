@model IEnumerable<FestivalCMS.Models.Category>
           @foreach (var c in Model)
           {
               <li>
                   @Html.ActionLink(c.Name, "Index", "Articles",new { catID=c.ID},null)
               </li>
           }
